; It builds it up in reverse because 'cons' prepends
; to the front of the answer list.
; In the second case we are constructing a list and the
; square, which leads to a hierarchical tree, rather
; than a list, which should be built up by using the list
; as the 2nd argument.

