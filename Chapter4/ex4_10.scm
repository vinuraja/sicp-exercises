#lang sicp
; You can change the syntax for let's say
; function definition by naming it function
; instead of define. So you could write:
; (function (add x y)
;   (+ x y))

; You could have then and else for if conditions:
; (if (> x y)
;   (then (...))
;   (else (...)))